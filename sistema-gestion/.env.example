# Environment variables declared in this file are available at build-time and run-time.
# Don't commit this file to source control when it contains secrets.

# Base de datos (PostgreSQL para producción)
DATABASE_DATABASE_URL="postgresql://username:password@hostname:port/database"

# Para desarrollo local (SQLite)
# DATABASE_URL="file:./dev.db"

# Prisma (para builds en Vercel)
PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING="1"

# Autenticación JWT
JWT_SECRET="tu-jwt-secret-muy-seguro-aqui"

# Next.js / NextAuth
# NEXTAUTH_URL="https://claudiahouse-nahuel-casagrandes-projects.vercel.app"
NEXTAUTH_SECRET="tu-nextauth-secret-muy-seguro-aqui"
NEXTAUTH_URL="http://localhost:3000"

# Configuración de impresión
PRINTER_NAME="default"

# API Keys (si necesitas integraciones externas)
# MERCADOPAGO_ACCESS_TOKEN=""
# AFIP_CUIT=""

# Google Drive API Configuration para carga de imágenes móvil
# Obtener estas credenciales desde Google Cloud Console
# 1. Crear un proyecto en Google Cloud Console
# 2. Habilitar Google Drive API
# 3. Crear credenciales de cuenta de servicio
# 4. Descargar el archivo JSON de credenciales
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_PRIVATE_KEY_ID=your-private-key-id
GOOGLE_CLOUD_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour-private-key\n-----END PRIVATE KEY-----\n"
GOOGLE_CLOUD_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_CLOUD_CLIENT_ID=your-client-id
GOOGLE_CLOUD_CLIENT_X509_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com

# ID de la carpeta de Google Drive donde se guardarán las imágenes (opcional)
# Si no se especifica, se guardará en la raíz del Drive
GOOGLE_DRIVE_FOLDER_ID=your-folder-id
